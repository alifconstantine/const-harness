# Replay counterpart to cordis.yml for the shipped Ralph-loop snapshot.
- id: base
  name: '@const-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@const-ai/llm-deepseek'
        disabled: true
      - insert:
          - id: llm-replay
            name: '@const-ai/llm-replay'
