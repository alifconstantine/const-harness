# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@const-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@const-ai/goal'
          - id: tool-goal
            name: '@const-ai/tool-goal'
