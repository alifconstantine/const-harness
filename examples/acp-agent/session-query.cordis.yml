# Explicit session-query tool opt-in for the dedicated spill scenario. The
# nested filesystem overlay supplies private spill storage and its byte limit.
- id: base
  name: '@const-ai/cordis-plugin-include'
  config:
    path: ./fs.cordis.yml
    patches:
      - insert:
          - id: tool-session-query
            name: '@const-ai/tool-session-query'
          - id: timeout-policy
            name: '@const-ai/tool-call-timeout-policy'
