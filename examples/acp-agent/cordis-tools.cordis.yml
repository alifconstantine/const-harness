# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@const-ai/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@const-ai/cordis-host-runner'
          - id: tool-cordis
            name: '@const-ai/tool-cordis'
