# Test-only composition of both public opt-in providers and foreground tools.
# The owning e2e boots this tree but never invokes a model or product process.
- id: fixture
  name: './fixture.ts'

- id: subagent
  name: '@const-ai/subagent'

- id: subprocess
  name: '@const-ai/subprocess-local'

- id: subagent-codex
  name: '@const-ai/subagent-codex'

- id: subagent-claude-code
  name: '@const-ai/subagent-claude-code'

- id: tool-subagent-codex
  name: '@const-ai/tool-subagent'
  config:
    provider: codex
    toolName: subagent_codex
    enableRunInBackground: false
    maxDepth: 'provider-managed'

- id: tool-subagent-claude-code
  name: '@const-ai/tool-subagent'
  config:
    provider: claude-code
    toolName: subagent_claude_code
    enableRunInBackground: false
    maxDepth: 'provider-managed'

- id: agent-spine
  name: '@const-ai/agent-spine-demo'
  config:
    agents:
      - id: main
        provider: mock
        model: mock-delegate
        cwd: !!js process.cwd()
    persona: 'This composition test must not start a model turn.'
    workspaceContext: false

- id: persistence
  name: '@const-ai/session-persistence-jsonl'
  config:
    root: './.sessions'

- id: checkpoint-policy
  name: '@const-ai/session-checkpoint-policy'
